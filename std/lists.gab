:use 'list'

def each[.List]
  self:len == 0 and return

  v = 0 
  loop
    yield self:at(v), v
    v = v + 1
  until v >= self:len end
end

def list.collect[](*stream)
  def list = :list.new
  
  for val in stream:splat
    list:push!(val)
  end
  
  list
end
