impl Number
  def each
    let v = 0

    loop
      yield v
      v = v + 1
    until v == self
  end

  def times(cb)
    let v = 0

    loop
        cb(v)
        v = v + 1
    until v == self
  end
end
