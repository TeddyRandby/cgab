'list':require
def Collection = 'Collection':require

def each[.List]
  self:len == 0 and return

  v = 0 
  loop
    yield self:at(v), v
    v = v + 1
  until v >= self:len end
end

Collection(.List)
