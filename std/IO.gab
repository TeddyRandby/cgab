'symbol':require
'fiber':require

impl .IO
    def read(path, config)
        return :fiber do
            def io = 'io':require

            status, fd = io:open(path, 'r')

            status == .ok or return status

            return fd:read
        end
    end
end

:symbol('IO')
