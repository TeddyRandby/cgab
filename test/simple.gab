:use .records
:use .streams

..res, sus = [0, 1, 2, 3, 4, 5]:filter(
  &:pairs,
  => @1 % 2 == 0,
)

{ res, sus }:print

..res, sus = [0, 1, 2, 3, 4, 5]:take(
  &:filter,
  &:pairs,
  => @1 % 2 == 0,
  2,
)

{ res, sus }:print

..res, sus = sus()

{ res, sus }:print
