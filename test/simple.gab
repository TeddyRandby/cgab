nil:use .dis

do specs, separator:
    specs:
      tuple.collect (
          \map,
          \values,
          \spec.explain,
          specs:len,
        ):
      join (
          \values,
          separator,
          \+,
        )
end :dis
