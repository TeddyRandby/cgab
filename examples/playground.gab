:use 'std/Any'
:use 'std/Pry'
:use 'dis'

def Vector { x y }

def + [Vector](other)
    other:is(Vector) or .INVALID_ARGUMENTS:panic

    {
        x = self.x + other.x
        y = self.y + other.y
    }
end


def print(..args)
  args:print
end

print:dis

print(1,2,3)
