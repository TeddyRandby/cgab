def KEY = $stack

def Stack {
  top
  data
}

def make[KEY]
  { top: 0 data: [] }
end

def push[Stack](element)
  self.data[self.top] = element
  self.top = self.top + 1
end

def pop[Stack]
  self.top = self.top - 1
  self.data[self.top]
end

def empty[Stack]
  self.top < 1
end

return KEY 
