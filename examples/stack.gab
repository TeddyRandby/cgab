def Stack {
  top
  data
}

def key = $stack

def make[key]()
  {
    top: 0
    data: []
  }
end

def push[Stack](element)
  self.data[self.top] = element
  self.top = self.top + 1
end

def pop[Stack]()
  self.top = self.top - 1
  self.data[self.top]
end

return key
