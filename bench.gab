require('std')

def impl_one = do
    for a in 5000:to:each
        a * a * a / a
    end
end

def impl_two = do
    for a in 50000:to:each
        a * a * a / a
    end
end

impl_one:timeit{ runs = 20 against = impl_two }
